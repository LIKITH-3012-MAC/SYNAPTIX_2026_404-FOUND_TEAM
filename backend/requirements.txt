# ================================================================
# RESOLVIT — Backend Dependencies
# Generated for Python 3.10+ (works on Python 3.13)
# Install: pip install -r requirements.txt
# ================================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.109.2
uvicorn==0.27.1          # NOTE: uvloop removed — it's Linux-only, breaks macOS
starlette==0.36.3

# ── Auth & Security ───────────────────────────────────────────
python-jose==3.3.0       # JWT encode/decode (JOSE spec)
passlib==1.7.4           # Password hashing
bcrypt==4.0.1            # bcrypt backend for passlib (compatible with Python 3.13)
cryptography==41.0.7     # Required by python-jose

# ── Database ──────────────────────────────────────────────────
psycopg2-binary==2.9.9   # PostgreSQL adapter

# ── Validation & Config ───────────────────────────────────────
pydantic==2.6.1
pydantic-settings==2.2.1
pydantic_core==2.16.2
python-dotenv==1.0.1
email-validator==2.3.0   # Required for pydantic EmailStr

# ── Scheduling ────────────────────────────────────────────────
APScheduler==3.10.4
pytz==2025.2
tzlocal==5.3.1

# ── ML / Data ─────────────────────────────────────────────────
numpy==1.26.4
scikit-learn==1.4.0
scipy==1.12.0
joblib==1.3.2

# ── HTTP Client ───────────────────────────────────────────────
httpx==0.26.0

# ── Utilities ────────────────────────────────────────────────
python-multipart==0.0.9  # Required for file/form uploads
annotated-types==0.7.0
typing_extensions==4.15.0
anyio==4.12.1
sniffio==1.3.1
h11==0.16.0
click==8.3.1
